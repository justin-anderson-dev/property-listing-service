// top-line property info with location, host, key features

import React from 'react';

const Summary = (props) => (
  <div className = "listing-summary">
    <h3>This is the Summary component.</h3>
    Top-level summary details go here.
  </div>
);

export default Summary;